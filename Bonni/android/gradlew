#!/bin/sh
# Wrapper that injects AAPT2 override for macOS arm64 then runs the real Gradle.
# See run-gradlew-aapt2.sh. Original script is gradlew.real.
exec "$(cd "$(dirname "$0")" && pwd)/run-gradlew-aapt2.sh" "$@"
